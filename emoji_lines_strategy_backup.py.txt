Line 83:         print(f"   📍 Countries: {len(classification)}")
Line 125:             print(f"   🌍 {region}: {len(countries)} countries")
Line 188: print(f"\n📋 VARIABLES AVAILABLE FOR ANALYSIS:")
Line 452:                     print(f"  ✓ Inverted {factor_name} (lower was better)")
Line 457:                     print(f"  ✓ Kept {factor_name} as-is (higher is better)")
Line 518:                         print(f"    ✓ {metric_name}: {coverage:.1%} coverage, {stacked.notna().sum()} valid observations")
Line 520:                         print(f"    ✗ {metric_name}: {coverage:.1%} coverage (below {self.min_data_coverage:.1%} threshold)")
Line 523:                     print(f"    ✗ Error processing {metric_name}: {e}")
Line 525:                 print(f"  ✗ Skipping {metric_name}: empty or invalid DataFrame")
Line 609:             print(f"✓ Processed {dates_processed} dates, {dates_with_data} dates had valid data")
Line 678:         print(f"✓ Created factor matrix: {final_data.shape[0]} observations × {final_data.shape[1]} factors")
Line 769:         print(f"✓ Found {len(high_corr_pairs)} highly correlated pairs (|r| >= {self.correlation_threshold})")
Line 864:         print(f"✓ Clustering reduced {len(correlation_matrix.columns)} factors to {len(selected_factors)}")
Line 969:         print(f"✓ VIF analysis: {len(candidate_factors)} → {len(remaining_factors)} factors")
Line 1026:         print(f"✓ Category balancing: {len(selected_factors)} → {len(balanced_factors)} factors")
Line 1210:         print(f"✓ Final matrix shape: {final_matrix.shape}")
Line 1475:    print(f"✓ Comprehensive report saved to: {output_file}")
Line 1509:                 print(f"  ✓ Exported {factor}")
Line 1511:                 print(f"  ✗ Failed to export {factor}: {e}")
Line 1526:         print(f"  ✓ Summary saved to {summary_path}")
Line 1530:     print(f"\n✓ Export completed: {exported_count}/{len(selected_factors)} factors")
Line 1531:     print(f"✓ Files saved to: {output_folder}/")
Line 1603:     print(f"✓ Validation completed:")
